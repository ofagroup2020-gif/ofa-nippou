<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
  <title>OFA｜管理者（検索）</title>
  <link rel="stylesheet" href="./style.css" />
</head>
<body data-page="admin" data-needlogin="1">
  <div class="wrap">
    <div class="topbar">
      <div class="brand">
        <div class="t">管理者｜検索・全データ出力</div>
        <div class="s">adminKey必須｜名前/電話/車両/フリーワード/日付範囲</div>
      </div>
      <button class="btn small" id="backBtn">戻る</button>
    </div>

    <div class="card" id="needLoginGate" style="display:none;">
      <div class="cardTitle">ログインしてください</div>
      <div class="note">トップページでGmailログイン後に再度お試しください。</div>
      <button class="btn primary" onclick="location.href='index.html'">トップへ</button>
    </div>

    <div class="grid">
      <div class="card half">
        <div class="cardTitle"><span>管理者キー</span><span class="pill">必須</span></div>
        <label>adminKey（トップで入力済みなら自動反映）</label>
        <input id="adminKey" type="password" placeholder="管理者パスワード" />
        <div class="note" style="margin-top:10px;">
          adminKey が正しい場合のみ、GASが「全データ検索」を許可します。
        </div>
      </div>

      <div class="card half">
        <div class="cardTitle"><span>検索条件</span><span class="pill">複合OK</span></div>

        <div class="row">
          <div>
            <label>開始日（from）</label>
            <input id="s_from" type="date" />
          </div>
          <div>
            <label>終了日（to）</label>
            <input id="s_to" type="date" />
          </div>
        </div>

        <div class="row">
          <div>
            <label>運転者名（部分一致）</label>
            <input id="s_name" type="text" placeholder="例：森下" />
          </div>
          <div>
            <label>電話番号（部分一致）</label>
            <input id="s_phone" type="text" placeholder="例：090" />
          </div>
        </div>

        <label>車両番号（部分一致）</label>
        <input id="s_vehicle" type="text" placeholder="例：OFA- / 480 / あ" />

        <label>フリーワード（メモ/日報/トラブル等も検索）</label>
        <input id="s_q" type="text" placeholder="例：クレーム / Amazon / 事故" />

        <button class="btn primary" id="btnDoSearch" style="margin-top:10px;">検索</button>
      </div>
    </div>

    <div class="card" style="margin-top:12px;">
      <div class="cardTitle">
        <span>検索結果</span>
        <span class="pill" id="searchCount">0件</span>
      </div>
      <div id="searchResult"></div>

      <div class="hr"></div>
      <div class="note">GAS: <span id="gasUrlView"></span></div>
    </div>
  </div>

  <div class="toast" id="toast"></div>
  <script src="./app.js"></script>
</body>
</html>
