<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"/>
  <title>OFA | 管理者</title>
  <link rel="stylesheet" href="./style.css"/>
  <script src="https://accounts.google.com/gsi/client" async defer></script>
</head>
<body data-page="admin">
  <div class="wrap">
    <div class="top">
      <div class="brand">
        <h1>管理者（全データ検索/出力）</h1>
        <p>管理者パスを入力 → 全データ検索（メール/氏名/電話/車番/ナンバー）</p>
      </div>
      <a class="pill" href="./index.html">← 戻る</a>
    </div>

    <div class="card">
      <div class="head"><p class="title">管理者解除</p></div>
      <div class="body">
        <div class="form">
          <label>管理者パスワード</label>
          <input id="adminPass" type="password" placeholder="管理者だけが知っているパス">
          <button class="btn dark" type="button" id="btnAdmin">管理者モードを有効化</button>

          <div id="adminBox" style="display:none;">
            <div class="hr"></div>
            <label>検索（メール / 名前 / 電話 / 車番 / ナンバー）</label>
            <input id="q" type="text" placeholder="例：森下 / 090 / OFA-12 / 鹿児島480">
            <button class="btn grad" type="button" id="btnSearch">検索</button>

            <div class="hr"></div>
            <div id="adminOut"></div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="toast" id="toast"></div>
  <script src="./auth.js"></script>
  <script src="./app.js"></script>
</body>
</html>
